# Qwen3.5-9B QLoRA 10-fold CV with INNER VALIDATION and NO CLASS BALANCING
# Training uses original imbalanced inner_train.jsonl.
# Final classification evaluation uses held-out test.jsonl outside this config.
# Test fold is not used during training or validation/perplexity monitoring.


model_path: "Qwen/Qwen3.5-9B"
method: "qlora"

training:
  learning_rate: 0.00005
  batch_size: 1
  gradient_accumulation_steps: 8
  num_epochs: 2
  max_steps:
  warmup_steps: 20
  max_grad_norm: 1.0
  weight_decay: 0.01
  fp16: false
  bf16: true
  gradient_checkpointing: true

lora:
  enabled: true
  r: 16
  alpha: 32
  dropout: 0.1
  target_modules:
    - q_proj
    - k_proj
    - v_proj
    - o_proj

quantization:
  enabled: true
  type: "nf4"
  compute_dtype: "bfloat16"
  double_quant: true

data:
  dataset_name: "data/qwen35_oversampling_10fold_cv/inner_splits/fold_09/inner_train.jsonl"
  dataset_split: "train"
  max_samples:
  max_length: 1024
  num_workers: 0

evaluation:
  eval_dataset: "data/qwen35_oversampling_10fold_cv/inner_splits/fold_09/validation.jsonl"
  eval_split: "train"
  eval_steps: 50
  eval_samples: 100
  compute_perplexity: true

checkpointing:
  output_dir: "outputs/qwen35_9b_qlora_unbalanced_10fold_cv_inner_val/fold_09"
  save_steps: 100
  save_total_limit: 3
  resume_from_checkpoint:

logging:
  logging_steps: 10
  log_level: "INFO"

device:
  device: "cuda"

advanced:
  seed: 42
